apiVersion: 2.0

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus-uid  #this is a unique identifier for the datasource, you can change it as per your requirement.
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"
      httpMethod: POST